{
  "name": "monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "setup": "npm install",
    "dev": "concurrently --kill-others --raw \"npm run dev --prefix apps/web\" \"npm run dev --prefix apps/api\" \"npm run dev --prefix apps/pocketbase\"",
    "build": "npm run build --workspaces --if-present",
    "start": "concurrently --kill-others --raw \"node apps/api/src/main.js\" \"npm run start --prefix apps/pocketbase\"",
    "start:api": "npm run start --workspace=apps/api",
    "dev:api": "node --watch apps/api/src/main.js",
    "lint": "concurrently --raw \"npm run lint --prefix apps/web\" \"npm run lint --prefix apps/api\""
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  },
  "workspaces": [
    "apps/*"
  ]
}